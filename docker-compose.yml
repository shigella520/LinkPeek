services:
  linkpeek:
    build: .
    ports:
      - "8080:8080"
    environment:
      BASE_URL: https://preview.example.com
      WEB_ICON_PATH: /data/favicon.svg
      CACHE_DIR: /data/cache
      STATS_DB_PATH: /data/stats/linkpeek.db
      CACHE_TTL_SECONDS: 86400
      CACHE_MAX_SIZE_GB: 10
      STATS_RETENTION_DAYS: 180
      DOWNLOAD_TIMEOUT: 120s
      PREVIEW_WARMUP_ENABLED: true
      PREVIEW_WARMUP_THREADS: 2
      PREVIEW_WARMUP_QUEUE_CAPACITY: 64
      VIDEO_MAX_QUALITY: 480
      LOG_LEVEL: INFO
    volumes:
      - ./data:/data
    restart: unless-stopped
